3.第三步则是去定义一个函数，这个函数就是按钮组件在点击时会去调用的。该函数内的作用就是去更改var变量的值，这样能够在点击按钮时改变显示的文字，代码示例如下所示：

def hit():
  var.set(’hello’)
4.第四步也是最后一步，使用Button()方法实例化出来一个按钮组件对象，在该方法内需要设置参数command的值为函数名。然后还是用pack()方法将其添加到窗口中，再启动窗口事件循环即可，详细代码如下：